fn main() {
    let mut sum: i32 = 0; //i32 default
    for i in -3..3 { //range -3,-2,-1.....1,2 #end point is excluded
        sum += 1
    }

    assert!(Sum == -3);

    for c in 'a'.. = 'z' { // a,b,c...,z
        println!("{}",c as u8);
    }
}
