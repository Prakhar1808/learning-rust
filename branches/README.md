some points i would like to remember:
repetition with loops
+ `break` keyword can be used to break/quit out of a loop
+ `continue` tells the program to skip over any remaining code in this iteration of the loop and move to the next iteration.
+ `return` always exits the current function
